fastapi>=0.68.0
uvicorn[standard]>=0.15.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0
python-multipart>=0.0.5
pymysql>=1.0.2
SQLAlchemy>=1.4.0
python-dotenv>=0.19.0
Pillow>=8.0.0
pydantic[email]
# Redis pub/sub fan-out so cross-pod WebSocket broadcasts land on every
# pod's locally-tracked WS connections. The official `redis` package
# ships an asyncio client (`redis.asyncio`).
redis>=5.0
