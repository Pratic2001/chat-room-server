# Placeholder Secret. Real values written by scripts/deploy_k8s.sh from
# app/.env.runtime. The Deployment references this Secret by name.
apiVersion: v1
kind: Secret
metadata:
  name: chatroom-app
  namespace: chatroom
  labels:
    app.kubernetes.io/name: chatroom
    app.kubernetes.io/component: app
type: Opaque
stringData:
  MYSQL_PASSWORD: "REPLACE_AT_DEPLOY_TIME"
  SECRET_KEY: "REPLACE_AT_DEPLOY_TIME"
  ROOM_SECRET_KEY: "REPLACE_AT_DEPLOY_TIME"
  MAIL_PASSWORD: ""
