apiVersion: v1
kind: ConfigMap
metadata:
  name: chatroom-config
  namespace: chatroom
data:
  # JWT
  ALGORITHM: "HS256"
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"
  # SMTP (password is in chatroom-secrets)
  MAIL_HOST: "smtp.gmail.com"
  MAIL_PORT: "587"
  MAIL_USER: "pratic61@gmail.com"
  MAIL_FROM: "Chat Room <no-reply@gmail.com>"
  MAIL_USE_TLS: "true"
